apiVersion: v1
kind: ServiceAccount
metadata:
  name: fushigi-scheduler-sa
  namespace: fushigi
  labels:
    app: fushigi
    run: scheduler
---
kind: Role
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  namespace: fushigi
  name: fushigi-scheduler-role
rules:
- apiGroups: ["batch"]
  resources: ["jobs"]
  verbs: ["create"]
- apiGroups: [""]
  resources: ["services"]
  verbs: ["create"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: fushigi-scheduler-rb
  namespace: fushigi
  labels:
    app: fushigi
    run: scheduler
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: fushigi-scheduler-role
subjects:
- kind: ServiceAccount
  name: fushigi-scheduler-sa
  namespace: fushigi
