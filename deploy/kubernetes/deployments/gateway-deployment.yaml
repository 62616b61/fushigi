apiVersion: apps/v1beta1
kind: Deployment
metadata:
  labels:
    app: fushigi
    run: gateway
  name: fushigi-gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fushigi
      run: gateway
  template:
    metadata:
      labels:
        app: fushigi
        run: gateway
    spec:
      containers:
      - image: kurai/fushigi/gateway
        name: gateway
