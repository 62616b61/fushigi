apiVersion: apps/v1beta1
kind: Deployment
metadata:
  labels:
    app: fushigi
    run: scheduler
  name: fushigi-scheduler
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fushigi
      run: scheduler
  template:
    metadata:
      labels:
        app: fushigi
        run: scheduler
    spec:
      containers:
      - image: kurai/fushigi/scheduler
        name: scheduler
