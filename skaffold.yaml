apiVersion: skaffold/v1beta1
kind: Config
build:
  artifacts:
  - image: kurai/fushigi/gateway
    context: ./gateway/
    sync:
      '*.js': .

  - image: kurai/fushigi/scheduler
    context: ./scheduler/
    sync:
      '*.js': .
deploy:
  kubectl:
    manifests:
    - ./deploy/kubernetes/*
